# Contributing to OpsNexus Common

Keep this module small and limited to stable cross-component contracts. Explain compatibility impact for every model or JSON-tag change and coordinate agent/backend/API consumers.

Run gofmt, go vet ./..., and go test ./.... Never add application logic, credentials, generated binaries, or backend-only models here.
